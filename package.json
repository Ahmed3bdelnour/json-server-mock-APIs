{
  "name": "json-server-mock-db",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch  social-db.json --port 3000",
    "start-store": "json-server --watch  store-db.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.21"
  }
}
